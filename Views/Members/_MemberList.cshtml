@model List<ZuHuanJingDemo2.Models.Member>


@if (Model != null)
{
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Member_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Member_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Member_Account)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Member_Email)
            </td>
            @*<td>
                @if (item.Is_Baned == 0)
                {
                    <label>否</label>
                }
                else
                {
                    <label>是</label>
                }
            </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.Member_CreateDate)
            </td>
            <td>
                <a asp-action="Edit" class="btn btn-primary" asp-route-id="@item.Member_Id">Edit</a>
                <a asp-action="Details" class="btn btn-info" asp-route-id="@item.Member_Id">Details</a>
                <a asp-action="DeleteConfirm" class="btn btn-danger" asp-route-id="@item.Member_Id" onclick="return confirm('確定要刪除嗎?')">Delete</a>
            </td>
        </tr>
    }
}